# 한국 기술사 통합 — 변경 요약

일본 시험 허브와 **별도 버튼**으로 갈랐습니다. 구조가 달라서입니다 —
덴켄·에관사는 「과목별 점수 → 과목 합격」이지만 기술사는 과목이 없고
31문 중 9문을 버리는 선택제 논술이라, 범용 프레임(constants-exams)에 넣으면
둘 다 망가집니다. 대신 **서브노트 토픽 체계에서 합류**시켰습니다.

## 새 파일

| 경로 | 역할 |
|---|---|
| `lib/constants-topics.ts` | 대주제 43개 + 2단 태그 규약. 기술사 ↔ 電験 좌표계 |
| `lib/constants-gisulsa.ts` | 종목 4개 + 교시 구조 + 회차 유틸 |
| `lib/data-gisulsa-balsong.ts` | 발송배전 130~139회 **310문항 · 113개 논점 태깅 시드** |
| `lib/gisulsaData.ts` | 시드·DB·덴켄 참조를 합치는 데이터 계층 |
| `app/dashboard/gisulsa/page.tsx` | 한국 기술사 허브 |
| `app/dashboard/gisulsa/[jong]/page.tsx` | 종목별 — 회차 목록·PDF·출제 분포 |
| `app/dashboard/gisulsa/[jong]/[exam]/page.tsx` | 회차 상세 — 문항 태깅 |
| `app/dashboard/gisulsa/subnote/page.tsx` | 서브노트 우선순위 보드 |
| `app/dashboard/gisulsa/subnote/[...tag]/page.tsx` | 논점별 서브노트 (TipTap 에디터) |
| `supabase/gisulsa_migration.sql` | 마이그레이션 |

## 고친 파일

`lib/constants-certs.ts` — CERTS 배열에 `gisulsa-kr` 한 줄 추가.
홈 상태 편집에서 진행중/예정/취득을 바꿀 수 있습니다. 그 외 수정 없음.

## 먼저 할 일

Supabase SQL 에디터에서 `supabase/gisulsa_migration.sql` 실행.
`gs_questions` / `gs_subnotes` 두 테이블과, `denken12_answers.topic_code`
컬럼 하나가 추가됩니다. 기존 테이블은 건드리지 않습니다.

## 덴켄과 어떻게 물리는가

덴켄 1·2종 풀이 화면에 이미 쌓인 `topic/keywords` 자유 텍스트를
**소급 태깅 없이** 끌어옵니다. `constants-topics.ts` 의 토픽마다 일본어 단서
(`jpKeywords`: シース, 対称座標, 調相設備 …)를 달아두고 문자열 매칭합니다.

- 흐린 보라 눈금 = 키워드 추정
- 진한 보라 눈금 = `denken12_answers.topic_code` 로 확정 태깅

즉 마이그레이션만 돌리면 지금 있는 덴켄 데이터로 바로 오른쪽 눈금이 그려지고,
나중에 코드를 확정해 넣으면 그쪽이 우선합니다.

## 나머지 세 종목

건축전기·전기응용·전기안전은 `GISULSA_SPECS` 에 자리를 잡아뒀습니다.
기출이 비어 있어도 화면이 열리고, **회차 번호 추가 → PDF 링크 → 문항 태깅**
순으로 채우면 같은 보드에 합쳐집니다. 회차 PDF는 새 테이블 없이
`denken_exam_docs` 를 `scope='gisulsa'` 로 재사용합니다.

시드를 코드로 넣고 싶으면 `lib/data-gisulsa-*.ts` 를 발송배전과 같은 형식으로
만들고 `lib/gisulsaData.ts` 의 `SEEDS` 에 연결하면 됩니다.

## 검증

`npx tsc --noEmit` 통과, `npx next build` 통과 (신규 라우트 5개 모두 컴파일).
ESLint 의 `set-state-in-effect` 경고는 기존 페이지들과 같은 패턴이라
일부러 맞춰 두었습니다.


## 2차 개정 — 코드 폐기와 논점 2단 구조

**바뀐 것 1. 코드를 버렸다.**
`A1` `B1` `G4` 에는 아무 정보가 없어서 대조표를 외워야 했다. 이제 이름이 곧 키다 —
`변압기`, `지중케이블`, `분산형전원연계`. 분류(`group`)와 電験 대응 과목(`jp`)은
별도 필드로 두고 화면에서 칩으로 보여준다. 구 코드는 `LEGACY_CODES` 에 대응표로 남겨뒀다.

**바뀐 것 2. 서브노트 단위를 논점으로 내렸다.**
대주제 43개는 분류지 작성 단위가 아니었다. 변압기 하나에 25문항이 걸리는데
그 안에 시험·정수 / 효율·손실 / 병렬운전 / 결선·각변위 / 냉각방식 / 보호가 다 들어 있다.
"변압기 공부해라"로는 한 장을 못 쓴다.

태그를 두 단으로 쓴다: `대주제/논점` (예: `변압기/병렬운전`).

- 대주제는 `TOPICS` 에 있는 것만. **電験 매칭이 이 단위로 걸린다**
- 논점은 자유 문자열. 미리 정한 목록이 아니라 기출에서 실제로 나온 것만 쌓이고,
  태깅 화면에서 기존 목록을 골라 쓰거나 새로 쓴다
- 논점 없이 대주제만 달아도 된다 (문항이 적은 대주제는 쪼갤 이유가 없다)

130~139회 310문항을 이 기준으로 재태깅했다. 8문항 이상인 18개 대주제를 쪼개
**231문항이 논점을 갖고, 전체 서브노트 장수는 113장**이 됐다.

**하지 않은 것 — 경향 예측.**
연 3회에 출제 범위가 넓어 15개년을 모아도 예측은 무리다. 여기서 하는 건
「주제 분류와 출제 빈도 정리」까지고, 화면 문구도 그 선을 넘지 않게 썼다.

**電験 참조가 논점보다 넓게 걸리는 이유.**
일본어 키워드 매칭은 대주제 단위로만 신뢰할 수 있다. 記述 한 문항이 논점 여러 개에
걸치는 일이 흔해서, 논점까지 좁히면 아예 안 걸린다. 서브노트 화면의 電験 목록은
「대주제 기준」이라고 명시해 뒀다.


## 3차 개정 — 에디터 제거, 기출 전문 추가

**바뀐 것 1. 서브노트 에디터를 뺐다.**
Overleaf 로 쓰고 구글 드라이브 PDF 링크만 거는 방식으로 교체했다.
TipTap 에디터는 LaTeX 조판을 대체하지 못하면서 저장 위치만 하나 더 늘리는 셈이었다.

- `gs_subnotes.pdf_url` 컬럼 추가 (마이그레이션 재실행 필요)
- 드라이브 공유링크는 `/view` → `/preview` 로 바꿔 화면 안에 바로 미리보기
- `body` 컬럼은 레거시로 남겨두되 화면에서 안 쓴다

**바뀐 것 2. 문항을 누르면 기출 전문이 나온다.**
요약문만으로는 소문항 구성과 주어진 조건을 알 수 없어서, 답안 골격을 못 잡는다.
130~139회 **310문항 전문을 전사해 넣었다** (`lib/data-gisulsa-balsong-text.ts`).

- 그림·표가 딸린 문항(회로도, 특성곡선, 계통도 등)은 `⚠ 그림·표 있음` 배지가 붙고,
  해당 회차 문제지 PDF 링크를 같이 건다 — 전문만으로는 못 푸는 문항이라서
- 서브노트 화면 / 보드 드로어 / 회차 상세 세 곳 모두 같은 `QuestionCard` 를 쓴다
- 새 회차는 회차 상세에서 태깅할 때 요약만 넣어도 되고, 전문은 위 파일에
  `'회차-교시-번호': [그림유무, '전문']` 한 줄로 추가하면 된다

## 공기업 필기 — 과목명 수정 버그 + 참고 프리셋

**고친 것. 과목명이 안 고쳐지던 문제.**
`NCS 직업기초` / `전공 (직무수행)` 두 줄이 하드코딩 라벨이었고, 추가 과목은
`extra_*` 컬럼 하나뿐이라 한 개까지만 가능했습니다. 기업마다 과목 이름이
다르고(한전 「직무능력검사」, 코레일 「직업기초능력평가」) 3과목 이상인 곳도
있어서 셋 다 열었습니다.

- `kp_ncs.ncs_label`, `kp_ncs.major_label` 추가 — 두 과목 이름 편집 가능
- `kp_ncs.extras jsonb` 추가 — 추가 과목 개수 제한 없음(＋과목 추가 / × 삭제)
- 기존 `extra_*` 에 든 값은 읽을 때 `extras[0]` 으로 자동 이관 (데이터 안 잃음)
- 총 문항수·시간·초당페이스 계산이 전부 `extras` 를 합산하도록 갱신

마이그레이션 재실행 필요: **`supabase/koreapub_ncs_books_migration.sql`**
(`add column if not exists` 라 안전. 지난 회차의 `supabase/gisulsa_migration.sql`
— `gs_subnotes.pdf_url` — 를 아직 안 돌렸다면 그것도 같이)

**추가한 것. 기업별 참고 프리셋.**
`lib/constants-koreapub-presets.ts` 에 10개 기업 값을 넣었습니다.
기업 카드를 열면 「📋 조사표 참고값이 있습니다 → 불러오기」가 뜹니다.

다만 원래 이 테이블에 시드를 넣지 않은 이유는 그대로 둡니다 —
*미리 채워두면 "확인했다고 착각한 값"이 남는다.* 그래서:

- 자동으로 안 들어갑니다. 버튼을 눌러야 들어옵니다
- 불러온 행은 `memo` 에 `⚠ 참고 프리셋 — 공고 미확인` 이 박히고
  목록 카드에 **⚠ 미확인** 배지가 뜹니다
- 공고로 확인한 뒤 메모의 ⚠ 문구를 지우면 배지가 사라집니다

출처는 공식 공고가 아니라 별도 조사 자료입니다. 특히 문항수·시간, 영역 구성,
전공 범위(법규 추가/제외)는 회차마다 바뀌므로 반드시 공고로 대조하세요.


## 공기업 필기 — 영역 선택을 칩 토글로

**뺀 것. 영역별 문항수 입력칸.**
PSAT형·피듈형은 공고가 영역별 문항수를 아예 안 밝히고, 설령 안다 해도
결국 전 영역을 다 공부해야 한다. 채워질 일이 없는 빈칸이 열 개 있으면
"채워야 할 것"처럼 보이기만 한다.

`NcsCell.q` 는 예전 값을 읽기 위해 타입에만 남겨뒀고, 화면에서는 안 쓴다.
매트릭스 칸도 숫자 대신 ● / · 로 켜짐 여부만 표시한다.

**넣은 것. 영역 토글 칩.**
10개 영역을 버튼으로 늘어놓고 눌러서 켜고 끈다. 켜지면 파란색으로 찬다.
공고에서 필요한 건 「무엇이 나오는가」뿐이고, 그 답이 한 화면에 다 보인다.

**가독성.**
패널 라벨이 10px/gray-600 이라 안 읽혔다. 11px/gray-400 으로 올리고
보조 문구도 gray-700 → gray-500 으로 밝혔다.


## 공기업 필기 — 프리셋을 기본값으로, 개수 프리셋 제거

**바뀐 것 1. 「불러오기」 버튼을 없애고 그냥 채워둔다.**
버튼을 눌러야 들어오는 방식은 한 번 누르면 패널이 계속 남아 거슬렸고,
어차피 공고 보고 고칠 값이라 굳이 잠가둘 이유가 없었다.

`rowOf()` 가 저장된 행이 없을 때 조사표 프리셋을 기본값으로 돌려준다.
**DB에 쓰지는 않는다** — 화면에만 채워두고, 고쳐서 저장할 때 비로소 행이 생긴다.
그래서 "안 건드린 기업"과 "확인해서 저장한 기업"이 여전히 구분된다.

미확인 표시는 남겼다. 목록의 ⚠ 배지 + 패널 상단 안내 한 줄.
메모의 ⚠ 문구를 지우면 사라진다.

**바뀐 것 2. 「3영역 / 5영역 / 6영역 / 전체」 버튼 제거.**
같은 6영역이어도 어느 6개인지가 기업마다 달라서, 개수만 맞춰봐야 결국
전부 다시 고쳐야 했다. `AREA_PRESETS` 상수도 지웠다.
「다른 기업에서 복사」는 계열사처럼 시험이 같은 곳끼리 쓸모가 있어 남겼다.


## 공기업 필기 — 반영비율을 문항수와 분리

문항수 비율과 필기 반영비율이 어긋나는 기업이 있다. 한전KPS 는 NCS 50문·전공 50문으로
문항수는 1:1 인데 반영은 100:50(2:1)이다. 문항수만 보고 시간을 반씩 나누면 손해다.
그런데 그동안 반영비율을 넣을 칸 자체가 없었다.

- `kp_ncs.ncs_score`, `kp_ncs.major_score` 추가 · `extras[].score` 추가
- 과목 줄이 **이름 · 문항 · 분 · 반영비율** 4칸이 됐다
- 값은 공고에 적힌 그대로 넣는다. `40 / 60` 처럼 %로 넣어도 되고
  `100 / 50` 처럼 배점으로 넣어도 된다 — 합계로 나눠 비율을 내므로 단위는 무관하다
- 미리보기에 과목별 **문항 % / 반영 % / 문항 하나의 값(배수)** 을 나란히 표시
- 둘이 5%포인트 넘게 갈리면 패널에 ⚠ 경고, 목록 카드에 **비율≠문항** 배지

마이그레이션 재실행 필요: `supabase/koreapub_ncs_books_migration.sql`

## N제 교재 — 電力 추가

`lib/constants-textbook.ts` 에 `denryoku` 과목과 12단원(1~175번)을 추가했다.
라우트(`/dashboard/textbook/[subject]/[chapter]`)와 DB(`textbook_problems`)가
이미 과목 슬러그로 일반화되어 있어 상수 한 곳만 늘리면 된다. 코드 변경 없음.

단원 구성은 「발전 → 송변전 → 배전 → 재료」로 설비를 따라 내려간 뒤
뒤쪽에 계산이 몰리는 형태다.

| 단원 | 범위 | 문제 |
|---|---|---|
| 수력발전 | 1~12 | 12 |
| 화력발전 | 13~42 | 30 |
| 원자력발전 | 43~56 | 14 |
| 그 외의 발전 | 57~68 | 12 |
| 변전소 | 69~89 | 21 |
| 송전 | 90~101 | 12 |
| 배전 | 102~117 | 16 |
| 지중전선로 | 118~125 | 8 |
| 전기재료 | 126~140 | 15 |
| 전력계산 | 141~168 | 28 |
| 선로계산 | 169~172 | 4 |
| 전선 이도와 지선 | 173~175 | 3 |

번호가 끊김 없이 1~175로 이어지는 것을 확인했다. 마이그레이션 불필요.
